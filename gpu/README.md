# Virtual GPU

## Requirements

- Virtual vram of ~256MB? - Needs calculations of what optimal size would be better, to leave some for programs and cpu
- Memory is shared with general ram, and is a logical section rather than separate buffer
- MMIO registers?
